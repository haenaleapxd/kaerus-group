<!DOCTYPE html>
<html class="{{ templateProps.className }}" {{ fn('language_attributes') }}>
  <head>
    <meta charset="{{ site.charset }}"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <link rel="profile" href="http://gmpg.org/xfn/11"/>
    {{ function('wp_head') }}
  </head>

  <body>
    {{ do_action('xd_body') }}
    {{ post.content }}
    {{ function('wp_footer') }}
  </body>
</html>
